kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  extraPortMappings:
  - containerPort: 30000 # Mapearemos el Service de K8s a este puerto
    hostPort: 9000
    protocol: TCP